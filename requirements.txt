# Audit A3 (Sprint A-1): Versionen gepinnt für Reproduzierbarkeit.
# Das System verspricht bit-identische Ergebnisse für identische Seeds —
# unfixierte Dependencies brechen dieses Versprechen über Major-Updates.
# Bei jedem Upgrade neu evaluieren und CI-Lauf gegen die Regressions-Baseline
# verifizieren.

ortools==9.10.4067
pandas==2.2.3
numpy==2.1.3
requests==2.32.3
geopy==2.4.1
python-dateutil==2.9.0.post0

# Audit A4 (Sprint A-1): tzdata explizit für zoneinfo (DST-Korrektur M2).
# Ohne tzdata fällt distance.py still auf Standard-Time-Offsets zurück.
tzdata==2024.2

# Tests
pytest==8.3.3
pytest-cov==5.0.0
hypothesis==6.122.1

# REST-API (Sprint 2.12.6, optional — nur fuer tools/api.py):
#   pip install fastapi "uvicorn[standard]"
# Bewusst NICHT als harte Kern-Abhaengigkeit, da der Optimizer ohne HTTP-Service
# laeuft. Beim Aktivieren Versionen pinnen + CI-Lauf gegen die Baseline verifizieren.
fastapi==0.115.5
uvicorn==0.32.1
